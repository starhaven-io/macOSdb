name: CodeQL

on:
  push:
    branches:
      - main
    paths:
      - "Sources/**"
      - "Package.swift"
      - "Package.resolved"

concurrency:
  group: "codeql-${{ github.ref }}"
  cancel-in-progress: false

permissions: {}

jobs:
  analyze:
    name: Analyze
    permissions:
      contents: read
      security-events: write # required to upload SARIF results
    uses: starhaven-io/.github/.github/workflows/reusable-codeql.yml@1f76a911b7f4fea29eca5983901fa203f38bd303 # v2026.07.27.1
    with:
      languages: '["swift"]'
      runner: "macos-26"
      timeout-minutes: 30
      build-mode: "manual"
      build-profile: "swift-package"
